.nav {
  --nav-width: 100%;
  width: var(--nav-width);

  &.nav-layout-sidebar {
    --nav-width: 250px;
  }
}

::slotted([slot="brand"]) {
  flex-shrink: 0;
  display: flex;
}

::slotted([slot="menu"]) {
}

::slotted([slot="utilities"]) {
}
