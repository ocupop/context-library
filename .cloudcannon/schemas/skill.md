---
name: # Required: 1-64 characters, lowercase letters, numbers, and hyphens only. Must not start or end with hyphen, no consecutive hyphens.
description: # Required: 1-1024 characters. Describes what the skill does and when to use it.
license: # Optional: License name or reference to a bundled license file.
compatibility: # Optional: Max 500 characters. Environment requirements (intended product, system packages, network access, etc.).
metadata: # Optional: Arbitrary key-value mapping for additional metadata.
  author: # Example metadata field
  version: # Example metadata field
allowed-tools: # Optional: Space-delimited list of pre-approved tools the skill may use (experimental).
---
